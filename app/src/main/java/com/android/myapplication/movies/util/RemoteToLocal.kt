package com.android.myapplication.movies.util

class RemoteToLocal {
/*
    fun getMovieDetails(movieDetailsResponse: MovieDetailsResponse): MovieDetail {
        return MovieDetail(
            id = movieDetailsResponse.id,
            originalTitle = movieDetailsResponse.originalTitle,
            title = movieDetailsResponse.title,
            posterPath = movieDetailsResponse.posterPath,
            backdropPath = movieDetailsResponse.backdropPath,
            overview = movieDetailsResponse.overview,
            voteAverage = movieDetailsResponse.voteAverage,
            voteCount = movieDetailsResponse.voteCount,
            releaseDate = movieDetailsResponse.releaseDate,
            revenue = movieDetailsResponse.revenue,
            budget = movieDetailsResponse.budget,
            genres = movieDetailsResponse.genres,
            status = movieDetailsResponse.status,
            runtime = movieDetailsResponse.runtime
        )
    }

    fun getCastDetails(movieDetailsResponse: MovieDetailsResponse): List<Cast>? {
        val castResponse = movieDetailsResponse.creditsResponse
        return castResponse?.casts
    }
    fun getReviewDetails(movieDetailsResponse: MovieDetailsResponse): List<Review>? {
        val reviewResponse = movieDetailsResponse.reviewResponse
        return reviewResponse?.reviews
    }
    fun getTrailerDetails(movieDetailsResponse: MovieDetailsResponse): List<Video>? {
        val videoResponse = movieDetailsResponse.videoResponse
        return videoResponse?.videos
    }*/
}